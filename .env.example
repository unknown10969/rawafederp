# SQLite – single file, Hostinger-friendly (path relative to project root)
DATABASE_URL="file:./prisma/dev.db"

# NextAuth secret (change in production!)
NEXTAUTH_SECRET="your-secret-key-change-in-production"

# Required for NextAuth warnings + correct callback URLs
NEXTAUTH_URL="http://localhost:3000"

# Master admin bootstrap (used only if no users exist yet)
MASTER_USERNAME="Abaan"
MASTER_PASSWORD="Abaan@123"
